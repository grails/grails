h1. properties

h2. Purpose

Allows access to the domain class properties as a map and is typically used for [Data Binding|guide:dataBinding] to perform types conversion when set allowing properties to be set from request parameters.


h2. Examples

{code:java}
def b = new Book(title:"The Shining")
b.properties = this.params
{code}

